runtime: python39

entrypoint: gunicorn -b :$PORT app:app

env_variables:
  # Environment variables can go here (if you have any)

handlers:
- url: /static
  static_dir: static
- url: /.*
  script: auto



